let totalPlayed = 20280;
const totalPlayingTime = (totalTime, sessionTime) => (totalTime += sessionTime);

totalPlayed = totalPlayingTime(totalPlayed, 210);
totalPlayed = totalPlayingTime(totalPlayed, 210);
totalPlayed;

const totalPlayed = 20280;
const totalPlayingTime = (totalTime, sessionTime) => (totalTime += sessionTime);

totalPlayingTime(totalPlayed, 210);
totalPlayingTime(totalPlayed, 210);
totalPlayed;
